@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}


<div>
    <div>
        <ng-content></ng-content>
    </div>
    <div *ngIf="pagescount>1">
        <button (click)="Back()"  md-raised-button>Назад</button>
        <button md-raised-button *ngFor="let n of buttons let i=index" (click)="ChangePage(n)" >{{n}}</button>
        <button (click)="Next()"  md-raised-button>Вперед</button>
    </div>
</div>