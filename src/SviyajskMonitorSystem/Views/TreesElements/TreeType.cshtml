@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div>
    <div id="jstree" >

    </div>
    <div class="dropdown" >
        <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown"  >{{currenttype}}<i class="glyphicon glyphicon-menu-down" ></i></button>
        <ul class="dropdown-menu" >
            <li  *ngFor="let t of types let i=index">
            <button class="btn btn-link"  (click)="ChangeType(i)" >{{t}}</button>
            </li>
        </ul>
    </div>
    <button color="primary"
    md-raised-button (click)="InitRedactor()" >Создать дерево</button>
</div>